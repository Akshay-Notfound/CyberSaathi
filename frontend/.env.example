# =================================================================
# CyberSaathi Frontend - Environment Configuration
# =================================================================

# Leave blank in development to use the Vite dev proxy (see vite.config.js).
# In local dev, Vite automatically routes /api requests to http://127.0.0.1:8000
#
# For a production build pointed at a deployed backend (e.g. Render/Railway), set:
# VITE_API_URL=https://cybersaathi-api.onrender.com
VITE_API_URL=
